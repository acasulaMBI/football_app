{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Attach Node (Docker remoto)",
      "type": "node",
      "request": "attach",
      "address": "localhost",
      "port": 9229,
      "restart": true,
      "protocol": "inspector",
      "localRoot": "${workspaceFolder}",
      "remoteRoot": "/app",
      "skipFiles": [
        "<node_internals>/**"
      ]
    }
  ]
}
